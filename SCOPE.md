# SCOPE — what is / is not proved for the prize (JSP-000361)

## Headline target

- Required theorem: `divisor_set_limsup`
- Status at scaffold: declaration present with `sorry`.

## What the headline covers

For every infinite A⊆ℕ and every k∈ℕ, limsup_{x→∞} (max_{n<x} d_A(n)) / (∑_{a∈A, a<x} 1/a)^k = ∞.

## Prize rules reminder

Do not open award claim issues / PR TheJustinSunPrize/awards.
